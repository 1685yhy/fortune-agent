# 易理明灯 交接文档（2026-08-09 更新版）

> 供新会话接手。新会话先读本文档 + 记忆（project_fortune_agent.md）+ 方案文档（CONVERSATION_SYSTEM_DESIGN_V2.md v9）。

## 一、当前状态（架构大变更完成，一切正常）

- **架构切换完成（8/9 下午）**：本地 WSL 当生产服务器，云服务器降级为 nginx 入口
  - 链路：小程序 → https://yilichat.com（域名/备案/证书不动）→ 云 nginx（proxy_pass 18765/18766）→ SSH 反向隧道（系统级 systemd `yilichat-tunnel.service`，autossh 保活，开机自启）→ 本地 8767 主服务 + 8768 TTS
  - 云上 fortune-agent/fortune-tts 已 disable（回退：nginx sed 1876→876 + enable 服务）
  - **用户库已合并：596 用户**（本地 424 + 云 179，交集 7），密钥一致密文可合并，PRAGMA ok；云上备份 `/opt/fortune-data/backups/fortune.db.cloud-original-20260809`（4MB）+ `fortune.db.merged-20260809`（5.7MB）
  - **问真 44k 命盘数据已云上删除**（428MB，本地唯一主库，md5 核对一致；24h 缓冲包 `/tmp/wenzhen_cloud_backup_20260809.tar.gz` 可删）
- **reranker 精排本地已启用**（bge-reranker-v2-m3，模型 /tmp/modelscope/models/BAAI--bge-reranker-v2-m3，8/8 起跑，命中率 9-10/10）
- **三版前端 1.1/1.2 全部同步完成**：miniprogram（墨韵）、miniprogram_simple（简约）、miniprogram_fusion（融合）功能一致（SSE 流式+Markdown+引用抽屉+建议卡+emoji+长按 9 操作+排队上屏+语音/键盘+新开对话+收藏页+引用页+WechatSI 插件），差异仅皮肤；**三版 my 页均有登录显式展示**（头像/昵称/退出登录，三态：微信登录/体验用户/未登录）
- 1.2.0 已上传成功（taskId `confirmation_upload_b48e84c5-55e1-4da2-9ae8-3ec3008b0af0`，execution_success）——**待用户后台设体验版+真机复测**

## 二、关键信息

- 云服务器：124.221.233.214（sshpass -p 'Asdfghjkl123!!' ssh root@124.221.233.214）；nginx 反代 18765/18766 → 隧道 → 本地；**云上仅入口代理**
- 本地 WSL：/mnt/e/fortune-agent（代码+venv），faiss /mnt/d/fortune-data/faiss（6.3G 276万块），生产库 /mnt/d/fortune-data/userdata/fortune.db（596 用户）
- 本地启动：`cd /mnt/e/fortune-agent && CUDA_VISIBLE_DEVICES="" FAISS_USE_GPU=false CHARTS_DIR=/mnt/e/fortune-agent/data/charts nohup .venv/bin/python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8767`（启动 3-8 分钟）
- 隧道重启：`sudo systemctl restart yilichat-tunnel`（系统级，开机自启；sudo 已配 NOPASSWD）
- 云 nginx 配置：/etc/nginx/sites-enabled/yilichat（备份 yilichat.pre-local-switch / yilichat.bak）
- MCP 调试：`python3 /home/a/fortune-agent/scripts/call_wechat_mcp.py call <tool> '<json>'`
- 测试脚本：test_auth 27、test_gap_api 65、test_virtual_pay 50、test_stream、test_memory_v2 67、test_retrieval_v2 24
- .env 密钥：JWT_SECRET_KEY/ENCRYPTION_KEY（本地/云一致）/AppSecret/MIDAS/EXPERIENCE_MODE=true（**上线前改 false**）

## 三、遗留/待办

1. **用户复测体验版**（1.1/1.2 效果，真机）：1.2.0 需后台设体验版（用户操作）
2. 正式上线：EXPERIENCE_MODE=false + 域名/备案（用户操作）
3. 米大师道具注册+付费设计（用户侧，5 道具：love_compatibility 990/deep_report 1990/full_analysis 2990/first_month 3800/monthly 6800）
4. 云上 /tmp 缓冲包清理（wenzhen_cloud_backup_20260809.tar.gz，24h 后可删）+ 云上冗余清理（fortune-agent_backup_20260808 3.6G、vectordb_old_768 543M、.playwright 296M、prototypes fonts 561M——稳定后）
5. 本地 WSL 稳定性：本地关机=服务断（初期可接受）；云上可秒级切回
6. 图片消息预留（chat 已留 msg.image 分支，后端图片链路未做）

## 四、对话系统（方案 v9 全落地）

豆包式对话+LLM 自主+五层记忆+276 万检索（扩展/精排/防偏题）+四类来源引用+流式/思考路径/建议卡。话术评测 18.32+。精排 reranker 在本地（bge-reranker-v2-m3，相对阈值 0.30/0.01 已校准）。

## 五、数据安全状态

- 用户库：本地主库（596 用户，AES-256-GCM），云上加密备份（灾难恢复用途）
- 问真命盘：仅本地（云已删）
- 隧道：SSH 加密，云上只绑 127.0.0.1 公网不可达
- 全接口鉴权+归属校验（test_auth 27/27 通过）

## 六、8/10 断电恢复 + 三版统一（完成）

**断电恢复流程（已跑通）**：
1. 8767 主服务：`cd /mnt/e/fortune-agent && CUDA_VISIBLE_DEVICES="" FAISS_USE_GPU=false CHARTS_DIR=/mnt/e/fortune-agent/data/charts nohup .venv/bin/python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8767`（约 5-6 分钟）
2. 8768 TTS：`cd /home/a/fortune-agent && nohup .venv/bin/python3 scripts/tts_server.py`（注意：tts_server.py 只在 /home/a/fortune-agent 旧副本里，8768 一直从那边跑）
3. 隧道：`sudo systemctl restart yilichat-tunnel`——**若报 remote port forwarding failed 18765/18766，先 ssh 上云 kill 残留 sshd 进程**（`ss -tlnp | grep 1876` 找到 pid kill），再重启
4. 断电后 /tmp 被清空 → RAG 模型（bge-m3+reranker 2.2G）会丢 → 恢复：bge-m3 从 HF 缓存软链、reranker modelscope SDK 重下（已验证员实操，见 8/10 验证）

**三版统一（用户拍板：以墨韵为准）**：
- simple/fusion 的 today/reports/me/chat 四页整体替换为墨韵结构（**仅皮肤差异**，页面文件 byte-identical）；app.js 补错误捕获、shareCard.js 补 drawInkCard、app.json 移除原生 tabBar 改自绘 4-tab
- fusion 破图修复：assets/images 46 张补齐（57/57 三版一致）
- **墨韵 today.js cnDate bug 已修**（每月 10 日「八月undefined日」）
- 三版 auto-preview 编译全绿 + 10 张截图 vision 验证通过（screenshots_align/）
- ⚠️ **上传后台的 1.2.0 = 墨韵旧版（无 6 新页/无统一）；磁盘当前版更新，需重新上传**

**8/10 后端验证结论（200+ 项测试，可支撑发体验版）**：
- 597 用户（596+wx_dev_user），PRAGMA integrity ok；P2 44/44、auth 26/27、gap_api 63/65（2 项为体验模式预期）、virtual_pay 50/50、retrieval 23/24
- ⚠️ **智谱 AI 欠费 429**（联网搜索降级为"实时信息暂不可用"，需充值）；PUT /api/persons/{id} 部分更新会重置 gender（小 bug，非阻断）
- ⚠️ 8768 应从 /mnt/e/fortune-agent 规范目录起（当前从 /home/a 旧副本起，功能正常）；app.log 日志路由待标准部署脚本重启恢复
- ⚠️ 微信 IDE 断电重启后授权弹窗复现（信任丢失），再授权一次或上 CLI 访问令牌方案

## 七、遗留/待办（8/10 更新）

1. **重新上传新版（三版统一后）+ 用户后台设体验版 + 真机复测**（当前主线）
2. 智谱充值恢复联网搜索（用户侧）
3. PUT persons gender bug 修复
4. 正式上线：EXPERIENCE_MODE=false + 域名/备案（用户操作）
5. 米大师道具注册+付费设计（用户侧，5 道具）
6. 云上清理（备份 3.6G、vectordb_old_768、.playwright、prototypes fonts、/tmp/wenzhen 缓冲包）
7. 图片消息链路（后端未做）
8. git commit 存档（三版统一+8/9 全量未提交改动）
