"""解梦输出边界净化（k55 r4，与 k52 合规扫描咬合）。

背景：k52 的合规扫描（`tests/test_k52_compliance_scan.py`）把「用户可见文案面 +
面向输出的提示词面」零高风险词固化成门禁。k55 的规则层 gloss 是**语料逐字引文**
（如「本命年的人梦见月光，外出小心车祸损伤、血光之灾」），引文本身必须保持逐字
（否则 k55「gloss 逐字来自语料」的守卫与不变式测试会失效）。

因此采取**两层分离**（控制方 2026-09-18 裁决）：
- **存储层**：`src/engines/dream_rules.py` / 语料 —— **保持逐字原文，不改**；
- **输出层**：本模块在「送进 prompt / 呈现给用户」的文本上做**中性化替换**，
  语义不变、读起来自然，且替换值本身**不含任何高风险词**（有测试断言这一点）。

高风险词表与 k52 完全一致（19 词，只许加不许减）。
"""
from __future__ import annotations

# ⚠️ 与 tests/test_k52_compliance_scan.py::HIGH_RISK_WORDS 保持一致（词表只增不减）
HIGH_RISK_WORDS = (
    "占卜", "算命", "卜卦", "改运", "转运", "破财", "血光", "消灾", "化解",
    "法事", "开光", "辟邪", "驱邪", "招财", "旺财", "灵验", "大师", "改命",
    "化灾",
)

# 繁体/异体形态（k58 M-4）：语料里真实存在繁体写法（`破財`/`轉運`），
# 只扫简体 19 词会漏。词表只增不减 —— 这里单独列，不并入 HIGH_RISK_WORDS
# （后者与 k52 门禁的词表**逐一对应**，是门禁契约的一部分）。
TRADITIONAL_VARIANTS = (
    "改運", "轉運", "破財", "消災", "化災", "開光", "闢邪", "驅邪",
    "招財", "旺財", "靈驗", "大師", "避邪", "改命運",
)

# 全部风险形态（简体 + 繁体/异体）
ALL_RISK_FORMS = HIGH_RISK_WORDS + TRADITIONAL_VARIANTS


# 中性化映射：语义等价或更委婉的自然说法。
# 硬约束（测试断言）：**替换值不得包含任何高风险词**（否则净化会自我复发，
# 例如「消灾 → 化解困厄」会重新引入「化解」，故取「消除困厄」）。
NEUTRAL_MAP = {
    # 固定搭配优先（长词先替换）：否则「血光之灾」会被逐字换成「意外伤灾之灾」，
    # 读起来就是机翻腔 —— 修法是在表里加长词条，而不是改替换策略。
    "血光之灾": "意外伤灾",
    "破财的一天": "财务损失的一天",
    "化解灾难": "顺利解决",
    "占卜": "推演",
    "算命": "推演",
    "卜卦": "起卦推演",
    "改运": "调整",
    "改命": "调整",
    "转运": "运势转变",
    "破财": "财务损失",
    "血光": "意外伤灾",
    "消灾": "消除困厄",
    "化灾": "消除困厄",
    "化解": "顺利解决",
    "法事": "祈福仪式",
    "开光": "祈福",
    "辟邪": "避除不祥",
    "驱邪": "避除不祥",
    "招财": "聚财",
    "旺财": "财运向好",
    "灵验": "应验",
    "大师": "先生",
    # 繁体/异体（M-4；替换值同样不得含任何风险形态）
    "改運": "调整",
    "轉運": "运势转变",
    "破財": "财务损失",
    "消災": "消除困厄",
    "化災": "消除困厄",
    "開光": "祈福",
    "闢邪": "避除不祥",
    "避邪": "避除不祥",
    "驅邪": "避除不祥",
    "招財": "聚财",
    "旺財": "财运向好",
    "靈驗": "应验",
    "大師": "先生",
    "改命運": "调整",
}

# 长词优先（避免「改运」被「改命」之类的短词先吃掉；同长度按字典序稳定）
_MAP_ORDERED = sorted(NEUTRAL_MAP.items(), key=lambda kv: (-len(kv[0]), kv[0]))


def find_high_risk(text: str, include_traditional: bool = True) -> list:
    """返回文本中命中的高风险词。

    默认**含繁体/异体**（k58 M-4 扩表）；`include_traditional=False` 时
    与 k52 扫描口径完全一致（简体 19 词），便于与门禁对齐做对比。
    """
    words = ALL_RISK_FORMS if include_traditional else HIGH_RISK_WORDS
    return [w for w in words if w in (text or "")]


def neutralize(text: str) -> str:
    """把高风险词换成中性说法；未命中则原样返回（幂等）。"""
    if not text:
        return text
    out = text
    for word, repl in _MAP_ORDERED:
        if word in out:
            out = out.replace(word, repl)
    return out
