# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Project
# (scripts/ removed to track build_index.py)

# 依赖与备份（不提交：node_modules 曾被误跟踪 19M，已 git rm --cached）
node_modules/
miniprogram/node_modules/
miniprogram/app.wxss.bak-base64font
miniprogram_backup_*/
miniprogram_fusion/
miniprogram_simple/

# 截图与日志
logs/
screenshots*/
*.log

# 运行时生成物（不提交）
data/charts/
data/memory/*.json
# k82 必修4：报告 JSON 是**运行期产物**（含明文姓名 + 八字），绝不入库。
# 改前 data/reports/ 下有 4 个被跟踪的遗留 .json（合成 fixture，但形态与真实报告
# 一致），已 `git rm` 移除；这条 patterns 保证**下次不会再被 `git add .` 带进来**。
# 目录本身靠 data/reports/.gitkeep 保留。
data/reports/*.json

# 竞品数据大文件不入库（超 GitHub 100MB 限制）
data/competitor_data/*.jsonl

# 工作记录/证据目录（2026-09-14 补：分支 worktree 里无嵌套 .gitignore，曾被误提交）
.superpowers/
