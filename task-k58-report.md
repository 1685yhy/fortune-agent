# k58 批次报告：解梦单字覆盖缺口（M-1）+ k55 Minor 收口（M-2~M-5）

**分支**：`k58-dream-gap`（worktree `/home/a/k58-wt`，起点 `55ea3f7`）
**时段**：2026-09-20 01:33～（合规时段外）
**状态**：交付完成；**只提交、不合并、不推送、不碰生产**（生产向量库零写入）
**报告副本**：worktree 内 `task-k58-report.md`

---

## 0. 一句话结论

规则表 **275 条**（单字 83 条）；**裸「梦见X」三项非空率 2.2% → 100%**（同一批 46 个单字元素，
提升 45 个），**42 条对抗样本零误伤**、13 条正当口语形态零漏覆盖。
match 分支**全部来自语料统计**（可复现脚本 + Top-N 表 + 每条分支带覆盖量），
边界护栏由**通用词典 + 本批语料**双来源推导（不是人工挑词）。
k55 的四条 Minor 全部收口，四套测试 **174 passed**。

---

## 1. M-1 主项：单字意象的裸查询覆盖

### 1.1 统计方法（**不是人工挑词**，控制方硬指导）

新增 `scripts/k55_dream/collocation.py`（可独立运行、可对更大语料重跑）：

| 产物 | 定义 | 数据来源 |
|---|---|---|
| `head_forms(X)` | `梦见X` / `梦到X` / `梦见了X` 三种触发形态的覆盖条数 | **语料正文**里含该形态的条目数（实数，非估算） |
| `collocations(X)` | 含 X 的 2~3 字搭配，按覆盖条数降序取 Top-5 | **语料标题核心串**（76,815 个唯一核心串） |
| `guard_chars(X)` | 「X 只是前缀」的护栏字集合 | **① jieba 通用词典**（498k 词条，以 X 开头的二字词的第二个字，freq ≥10）**② 本批语料**（以 X 开头且自己是独立词条的核心串的第二个字） |

**三条数据驱动的过滤**（每条都能回答「这个分支哪来的」）：
1. 搭配若**本身是另一条规则名**（车祸/开车/火车…）→ 交给那条规则（避免重复命中与吉凶串味）；
2. 搭配若 `X+护栏字` 且**本身是通用词典词**（火车/酒店/山羊/猫头鹰/水杯）→ 丢掉（那是别的东西）；
3. 搭配若是碎片（`萄酒`）或与头部形态重复 → 丢掉。

**护栏为什么必要**：k49 教训是「单字条目前缀误伤」——`梦见火` 若不加护栏会命中 `梦见火腿`。
护栏把「X 后面跟的是构成别的东西的字」的情况挡在头部形态分支之外，而正当搭配
（水里/蛇咬/狗追）由搭配分支覆盖，所以**挡住不等于漏**。

### 1.2 改前 / 改后（同口径实测，脚本 `measure_single_char.py`）

| 指标 | 改前（`55ea3f7` 的规则表） | 改后 |
|---|---|---|
| 单字规则条数 | 46 | 83（语料扩量后候选变多） |
| **裸「梦见X」三项非空率（同一批 46 个元素）** | **2.2%（1/46）** | **100%（46/46）** |
| 提升个数 | — | **45 个** |
| 改后全部单字元素（含新增） | — | **97.6%（81/83）** |
| 未覆盖的 2 个 | — | `窝`/`崽`（语料里没有裸形态证据 → 按 k49 规则**不发裸单字分支**，如实留空） |

逐元素表：`reports/single_char_coverage.csv`（element / before / after / hits）。

### 1.3 Top 单字元素的搭配分支（**Top-N 表，全部带覆盖量**）

| 元素 | 分支（覆盖条数） |
|---|---|
| 水 | 梦见水(1140) 梦到水(60) 梦见了水(8) 水里(657) 在水(356) 在水里(278) |
| 车 | 梦见车(731) 梦到车(26) 梦见了车(4) 开车*(规则) 出车(121) 汽车(115) 火车(112) |
| 手 | 梦见手(1265) 梦到手(50) 梦见了手(17) 咬手(316) 分手(92) 蛇咬手(91) |
| 火 | 梦见火(811) 梦到火(24) 梦见了火(12) 被火(58) 大火(50) 被火烧(49) 起火(41) |
| 蛇 | 梦见蛇(5758) 梦到蛇(1280) 梦见了蛇(45) 蛇咬(1043) 被蛇(563) 见蛇(437) 被蛇咬(401) |
| 钱 | 梦见钱(397) 梦到钱(12) 梦见了钱(10) 给钱(70) 借钱(68) 钱给(62) 给我钱(48) |
| 鱼 | 梦见鱼(2707) 梦到鱼(285) 梦见了鱼(1) 多鱼(345) 鱼在(229) 抓鱼(213) 鱼咬(199) |
| 狗 | 梦见狗(3688) 梦到狗(160) 梦见了狗(5) 狗咬(625) 被狗(268) 狗追(238) 被狗咬(177) |
| 酒 | 梦见酒(181) 梦到酒(7) 喝酒(121) 醉酒(23) 葡萄酒(21) |
| 头 | 梦见头(536) 梦到头(31) 剪头(96) 剪头发(95) 头蛇(53) 掉头发(51) |

（完整 83 个元素：`reports/single_char_collocations.json` / `.csv`；
每条规则的 `match_branches` 字段里逐分支写着 kind + coverage，随规则模块落库。）

### 1.4 反例清单（k49 误伤评估，42 条「含该字但非该意象」→ **零误命中**）

脚本 `scripts/k55_dream/adversarial_check.py`（可重跑，结果落 `reports/adversarial_check.json`）：

| 分组 | 样本（期望**不**命中该字规则） | 结果 |
|---|---|---|
| 水 | 梦见水杯、水果、水泥、水龙头、水饺、矿泉水 | ✅ 0/6 误命中 |
| 火 | 梦见火腿、火车、火锅、火星、火药、打火机 | ✅ 0/6 |
| 车 | 梦见车门、车站、车牌、车票、车间 | ✅ 0/5 |
| 狗 | 梦见狗肉、狗熊、狗屁 | ✅ 0/3 |
| 马 | 梦见马路、马上、马匹 | ✅ 0/3 |
| 酒 | 梦见酒店、酒精、酒席 | ✅ 0/3 |
| 鱼 | 梦见鱼雷、鱼翅、鱼苗 | ✅ 0/3 |
| 猫/鬼/手/山/河/钱/刀 | 梦见猫头鹰、鬼话、鬼子、手巾、手册、手术、山顶、山羊、河马、河堤、钱币、刀法 | ✅ 0/12 |
| 荒谬串（允许命中，不许崩） | 梦见蛇精病 → `[]`；梦见火星人 → `[]` | ✅ 均未命中 |

**反向防漏**（护栏不能把该命中的也挡了）：13 条正当口语形态
（梦见蛇/大蛇/被蛇追、梦见水/发大水、梦见火/大火、梦见车/开车、梦见狗/被狗咬、梦见鱼/抓鱼）
**全部被覆盖**（规则层命中或元素层命中），0 漏。

### 1.5 M-1 实施中修掉的两个真 bug（实测踩到）

1. **`re.escape` 毁掉护栏**：单字元素的 match 分支里含 `(?!…)` 正则护栏，
   却被统一 `re.escape` 转义成字面量 → 分支永远匹配不上（改后一度只有 4.8% 覆盖率）。
   修法：单字元素的分支**原样拼接**，多字元素仍走 escape。
2. **重叠命中不去重导致吉凶串味**：`梦见出车祸了` 同时命中 `车`（靠搭配「出车」）
   与 `车祸`（命中「出车祸」）——两段重叠但非包含，两条都留 → 综合 luck 被
   `车`（凶多于吉）带偏，把 brief 指定的「车祸＝中性」冲掉。
   修法：重叠时**最长匹配优先**，同长再按「名称直命中 > 覆盖量小者（规则更具体）> 分数」。
   （另修：`回来` 这类趋向补语混进过规则表，已加入叙述词停用表。）

---

## 2. M-2 `死亡` 类空壳 gloss

**要求**：head_entry 选句跳过「纯出处句」（正文 <6 字且不含判词词）+ 全表扫同类。

**实现**（`build_rules.py`）：新增三个句子池闸门，全部在选句前生效：
- `is_citation_only()`：去掉书名号/标点后正文 <6 字且无判词字 → 丢（`《敦煌本梦书》` 这类）；
- `is_heading()`：页面小标题（`1. 梦见堵车的周公解梦：`、`梦见蛇的解析：`）→ 丢；
- `is_fragment()`：半截句（以虚词/系动词结尾且 <15 字，如 `吉祥色彩是`，页面在动词后折行造成）→ 丢。

**全表复核结果**：275 条规则的 gloss **0 条**命中以上三类（测试 `test_no_empty_shell_glosses` 锁住）。
实测改善样例：`堵车` 旧 `1. 梦见堵车的周公解梦：` → 新「恋爱中的人梦见堵车，说明亲人有意见，不可灰心，终有希望成婚」；
`迷路` → 「梦见自己找不到回家的路、迷路，说明最近你总是感到比较迷茫…」。
（`死亡` 本身在当前语料下已不在规则表内——语料扩量后候选集变了——但闸门是通用的。）

---

## 3. M-3 三条无方向档带极性引文

**口径**（数据驱动 + brief 允许的兜底限定）：无方向档（中性/提醒类）选句时
**优先无极性句**；若池中确无无极性句，则保留该引文但**追加限定语**「（传统说法，不代表吉凶；…）」，
与 k55-r3 的 prompt 口径一致。

**结果**（全表扫描，测试 `test_no_direction_rules_have_nonpolar_gloss_or_caveat` 锁住）：

| 规则 | luck | 改前引文极性 | 改后 |
|---|---|---|---|
| 堵车 | 中性 | 凶（"很不顺利"） | **无极性句**：「恋爱中的人梦见堵车，说明亲人有意见，不可灰心…」 |
| 坐车 | 中性 | 混合 | **无极性句**：「这个日子除了旅游，适合安静地度过…」 |
| 开车 | 中性 | 吉/混合 | **无极性句**：「这两天你的运势转差了些…工作方面仍要注意」（无判词极性） |
| （附带修）红色 / 墙 | 中性 | 凶 | 保留引文 + **限定语**「（传统说法，不代表吉凶；语料「梦见红色」词条原文）」 |

另修一个顺序 bug：限定语原先放在「反向降级」之前判，导致降级成中性的条目漏加限定语
（实测漏了 `红色/墙`）——已移到降级之后。

---

## 4. M-4 繁体/异体净化 + `解夢` 导航串清洗

- **净化表扩表**（词表只增不减）：新增 `TRADITIONAL_VARIANTS` 13 条
  （改運/轉運/破財/消災/化災/開光/闢邪/避邪/驅邪/招財/旺財/靈驗/大師/改命運），
  与简体 19 词合为 `ALL_RISK_FORMS`；映射同步补齐（替换值仍**零风险形态**）。
  实测：`梦见破財要轉運，大師說要開光驅邪` → `梦见财务损失要运势转变，先生說要祈福避除不祥`。
  注：`HIGH_RISK_WORDS` 仍与 k52 门禁逐词对应（门禁契约），繁体单列。
- **`解夢` 导航串**：`TITLE_JUNK_RE` 补繁体页面导航词（周公解夢/農民曆/黃曆/看相/十二星座/
  心理測試/姓名測試/血型性格/風水知識/民俗預測/抽籤/星座運勢/收起…）→
  该类整句导航不再可能当选 gloss（测试锁定 0 条小标题/套话）。

---

## 5. M-5 出口净化：测试锁 + **已知未覆盖出口清单**

**新增测试**：`test_dream_exit_sanitization_effective_including_traditional`
——解梦引擎的确定性出口（prompt + `symbols/element_notes/tones/rule_notes/luck_reason/
reality_projection/interpretations`）对 **19 词 + 13 个繁体异体** 零命中；
同时 `test_rules_table_still_verbatim_for_audit` 保证**存储层仍逐字**（两条同时成立）。

**已知未覆盖的出口清单（交控制方下批处理）**：
1. **规则模块本身**（`src/engines/dream_rules.py`）—— 任何**直接 import 并展示**
   `DREAM_PATTERN_RULES` 的新代码（后台/运营工具/新接口）都会拿到未净化的逐字引文；
2. **语料文件**（`k55_dream/clean/*.jsonl`）与**k55 独立向量库**（`vectordb_k55/dreams_k55`）
   —— 若将来入库到生产库并经由**其它检索出口**（非解梦）呈现，需各自接净化；
3. **导出/报表**：项目里没有解梦相关的导出入口（已 grep 确认），但若新增导出需接净化；
4. **日志/审计留痕**：`dao.save_consultation(...)` 存的是 `DreamResult`——
   当前保存的是**已净化后的字段**（净化发生在构造 DreamResult 时），故落库文本是净化态；
   但 `original_text`（用户原话）保持原样（这是刻意的：不改用户输入）；
5. **繁体词表覆盖**：本次扩了 13 个常见繁体异体，**不是全集**（如「蔔卦」「霛驗」等罕见异体未覆盖）。

**建议**：下批把「读取时净化」收敛成**统一入口**（如 `dream_text_output()`），
所有出口一律经它，而不是逐出口接净化。

---

## 6. 语料扩量披露（影响所有 coverage 数字的口径）

k55 阶段后台抓取持续到本次开工，语料从 38,411 条（k55 r1 时）涨到
**119,661 条原始 / 76,815 唯一核心串**（好梦网 detail 已抓 59,996 条）。
因此本批**重跑了整条统计链**（`stats_elements` → `collocation` → `build_rules`）：

| 指标 | k55 r1（旧语料） | k58（当前语料） |
|---|---|---|
| 规则条数 | 263 | **275**（候选覆盖阈值同步提到 ≥60） |
| 单字规则 | 46 | 83 |
| 元素候选（覆盖 ≥20） | 531 | **1,020** |
| Top1 元素 | 人(3338) | 人(7347) |

**含义**：本批所有 `coverage` 数字与 k55 报告里的**不可直接比**（语料规模不同）；
k60 阶段 2 扩量后需再算一遍（接口已按此设计：
`collocation.py` / `stats_elements.py` 都可对更大语料原样重跑）。

---

## 7. 测试

```
tests/test_k58_single_char.py        26 passed（新增：覆盖率/统计证据/护栏/对抗集/
                                               正当形态/空壳 gloss/无方向极性/繁体净化/出口净化）
tests/test_dream_rules_k55.py        81 passed（既有，未改宽）
tests/test_dream_engine_g4.py        39 passed（既有，一字未动）
tests/test_k52_compliance_scan.py    28 passed（合规门禁仍绿）
─────────────────────────────────────────────
合计                                174 passed
```
未跑全量 pytest（按纪律由控制方统一安排）。生产零写入。

---

## 8. 诚实披露 / 残留限制

1. **未覆盖的 2 个单字元素**（`窝`/`崽`）：语料里没有裸形态证据，按 k49 规则不发裸单字分支
   → 裸查询仍为空。这是**刻意**（宁可空，也不放单字分支去误伤）。
2. **口语句式仍会漏**：实测 `梦见开着车出去玩` 规则层未命中（`开车` 分支只有
   `开车|驾驶|自驾|开夜车|开快车`，`车` 的搭配里 `开车` 被让给了 `开车` 规则）。
   这类「插入助词」的口语形态没有语料证据可统计（标题里不出现），**未手工补**
   —— 记入下批可做项（需要从**真实用户输入**里统计，当前语料没有这一层）。
3. **护栏依赖 jieba 通用词典**：词典未收录的复合词（罕见词）可能仍被前缀误伤；
   对抗集里 42 条已全覆盖，但不等于「任意复合词都安全」。
4. **`match_branches` 里 head_form 分支的覆盖量口径**是「语料正文含该形态的条目数」，
   与规则 `coverage`（标题核心串含该元素的条目数）**不是同一个口径**，表内已分别标注字段名。
5. **M-5 未覆盖出口**见 §5 清单（规则模块直读 / 语料与独立向量库 / 导出 / 罕见繁体异体）。
6. 本批**改了引擎的匹配去重语义**（重叠 → 最长匹配优先）：这会让**同一输入的命中集合**
   与 k55 略有差异（已用 174 项测试护住既有契约；g4 的 39 项一字未改仍全绿）。
