# Fortune Agent Settings
data_dir: /mnt/d/fortune-data
claude_model: claude-opus-4-8
# claude_api_key: sk-ant-xxx  # 或通过环境变量 ANTHROPIC_API_KEY

# Daily push settings
push:
  enabled: true
  time: "08:00"              # 默认推送时间 (HH:MM)
  timezone: "Asia/Shanghai"
  max_users_per_batch: 50    # 每批最大推送用户数

# Admin
admin_key: "fortune-admin-2024"
